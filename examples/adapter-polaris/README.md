# adapter-polaris (example)

Reference adapter for Shopify Polaris. Authored against `@driftpatch/adapter-sdk` only — no internal engine imports. Same constraints a real FDE would face.

Lives in `examples/`, not `packages/`, because adapters are not part of the shipped tool.

## Status

Skeleton only. Parser implementation comes next, alongside fixtures from real Polaris changelogs.
