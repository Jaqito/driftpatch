{
  "name": "@driftpatch/eval",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@driftpatch/adapter-sdk": "workspace:*",
    "@driftpatch/core": "workspace:*"
  }
}
