# Changelog

## 13.0.0

- Removed `Card.subdued` prop; use `Card.tone="subdued"` instead
- Added `loading` prop to `Button`
- Renamed `TextStyle` to `Text`

## 12.5.0

- Deprecated `LegacyStack`; use `BlockStack` or `InlineStack`
- New default value for `Page.fullWidth` is `false`
