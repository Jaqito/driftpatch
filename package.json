{
  "name": "driftpatch",
  "version": "0.0.0",
  "private": true,
  "description": "Generic upstream-drift engine. Provider adapters and repo skills make it useful per-engagement.",
  "license": "SEE LICENSE IN LICENSE",
  "packageManager": "pnpm@9.12.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsup": "^8.3.5",
    "vitest": "^2.1.5",
    "@types/node": "^22.9.0"
  }
}
